dependencies {
    compileOnly project(':restful-server')
    compile group: 'com.google.inject', name: 'guice', version: guiceVersion

    compile group: 'com.auth0', name: 'jwks-rsa', version: '0.7.0'
}